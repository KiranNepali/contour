import BookMain from "@/components/website/BookPage/BookMain";
import React from "react";

type Props = {};

export default function page({}: Props) {
  return (
    <div>
      <BookMain />
    </div>
  );
}
